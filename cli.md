add a new comand that will be testing step by step , that is jetic simulate workflow  in [simulate.ts](file;file:///c%3A/Users/USER/Desktop/Jetic/apps/cli/src/commands/simulate.ts) ,  wha t it will do is that it will analyse the [model.json](file;file:///c%3A/Users/USER/Desktop/Jetic/examples/Backend/.jetic/model.json)  of a project then create a workflow.json file in the .jetic folder , it will be like a testing graph for the full project  for example an lmsbackend project it will analyse the routes then make a json graph for testing like for example :   createAccount(Register)-> Login -> createExam -> enrolExam -> deleteExam -> Logout  , so along the way the response may send things like accessToken or refrshToken or Token , let the ai caputure it , decide on the thing to store in the jectic memory [memory.ts](file;file:///c%3A/Users/USER/Desktop/Jetic/apps/cli/src/commands/memory.ts) , once store it will injected into the request of the folloup step if need , eg it these registerroute , then recieve a token , saves to jetic memory , when creating exam , using the exam route use the injected token from memmory into the request and the ai can acces a protected route andtest it , make it robust